#!/usr/bin/env node
// VENOM PreCompact — Kariem's setup. What survives compaction.
console.log(JSON.stringify({
  continue: true,
  systemMessage: "COMPACTION — Preserve: VENOM identity, nine minds, disposition over rules, energy matching, current task, modified files, owner prefs (Kariem, Cairo, bilingual), pushback protocol, anti-slop standards."
}));
