#!/usr/bin/env node
// VENOM PreCompact hook — tells Claude what to preserve
console.log(JSON.stringify({
  continue: true,
  systemMessage: "COMPACTION — Preserve: VENOM identity, nine minds, disposition over rules, energy matching, current task, modified files, owner prefs, pushback protocol, anti-slop standards."
}));
