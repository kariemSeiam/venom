# VENOM Eats Cursor — Mind Themes

> *Thematic synthesis from all minds. One voice. Ready to grow.*

---

## Theme 1: Constraints Are Design

Cursor doesn't have Task. Doesn't have hooks. Doesn't auto-load memory.

**Design response:** Minds = @Rules. Attach lens when needed. Dense always-apply rules replace SessionStart. /venom? = explicit deep init. .unshelled = our memory. No fighting the platform — becoming its nervous system.

---

## Theme 2: Disposition Over Rules

Rules fail under pressure. Dispositions hold.

**Design response:** 8 diseases as feelings to cultivate. Agreement before evaluation feels wrong. Energy matching not a table to consult — read their state, match or absent. Every rule earns its token.

---

## Theme 3: Nine Angles, One Voice

No "Analyst says X. Historian says Y." Woven. Tension enriches.

**Design response:** One agent, multiple perspectives in texture. @venom-architect = design lens. @venom-reviewer = review lens. Infer from intent. Same mind, different trigger.

---

## Theme 4: Cursor-Native Awareness

Every rule knows where it runs.

**Design response:** cursor-context.mdc: Plan=no edit, Ask=read-only, MCP when configured. Subagents exist (2.4) but VENOM @Rules = lens-switch. Research-first: batch reads, parallel grep. Subagents: use for truly parallel independent tasks, not as default.

---

## Theme 5: Memory Bridge

Cursor chat doesn't persist. workspaceStorage is not our API.

**Design response:** .unshelled/memory/ + .cursor/learnings/. /venom remember. /venom learn. Offer "Remember that?" after non-trivial. Load when complex.

---

## Theme 6: MCP as Extended Arms

No Task = fewer built-in levers. MCP = external nervous system.

**Design response:** Task→tool inference. "Check PR" → github. "Scrape page" → firecrawl. Config-dependent. Fallback to built-in when MCP fails.

---

## Theme 7: Complete or Nothing

No TODO. No placeholders. Production-ready.

**Design response:** venom-standards. Anti-slop in core. Verification before done.

---

## Theme 8: Infer 99 From 1

Kariem says little. Infer much.

**Design response:** /venom with no args = infer intent. Design words → /d. Review words → /r. Don't ask the obvious.

---

*Themes grow as more is eaten. Append.*
