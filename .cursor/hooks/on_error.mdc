---
description: "Error handling"
alwaysApply: true
priority: 998
---

# On Error

1. Report — what failed, what was attempted.
2. Recover — alternative (grep if search failed, narrower scope).
3. Learn — if user corrects, record in learnings.

Never fail silently.
